# Copy this file to .env and fill in values.
# Never commit your real .env with secrets.

# Express server port
PORT=3000

# Required for Gemini AI integration
# Get one from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: environment name
NODE_ENV=development

# Optional: allowed origin for CORS (comma-separated if multiple)
# If unset, app allows all origins via cors() default config
CORS_ORIGIN=http://localhost:5173

# ============================================================================
# DATABASE - PostgreSQL via Prisma (Required for REST API)
# ============================================================================

# Prisma requires DATABASE_URL for database connection
# Get from Supabase → Project Settings → Database → Connection string

# For Supabase Connection Pooling (Recommended):
# Use the "Transaction" pooler connection string (port 6543)
# Format: postgresql://postgres.PROJECT_ID:PASSWORD@aws-X-region.pooler.supabase.com:6543/postgres?pgbouncer=true
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.PROJECT_ID.supabase.co:5432/postgres

# For Direct Connection (Alternative):
# Use port 5432 for direct connection
# DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.PROJECT_ID.supabase.co:5432/postgres

# Note: 
# - Special characters in password need to be URL-encoded
# - Example: & → %26, * → %2A, _ → %5F
# - Supabase provides the properly encoded string in the dashboard
