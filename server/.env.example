# AI-Sihat Server Environment Configuration
# Copy this file to `.env` and fill in your values
# Note: This example uses PostgreSQL (required by Prisma schema)

# === DATABASE CONNECTION (REQUIRED) ===
# Format: postgresql://[user]:[password]@[host]:[port]/[database]?schema=public
# 
# Examples:
#   Local: postgresql://postgres:password@localhost:5432/ai_sihat_db?schema=public
#   Cloud (Supabase): postgresql://postgres:[password]@[project].supabase.co:5432/postgres?schema=public
#
DATABASE_URL="postgresql://postgres:password@localhost:5432/ai_sihat_db?schema=public"

# Optional: Direct URL for connection pooling (can be same as DATABASE_URL)
DIRECT_URL="postgresql://postgres:password@localhost:5432/ai_sihat_db?schema=public"

# === SERVER SETTINGS ===
PORT=3000
NODE_ENV=development

# CORS: Allowed origins, comma-separated (leave empty to allow all)
# Example: http://localhost:5173,http://localhost:3000
CORS_ORIGIN=http://localhost:5173

# === GOOGLE GEMINI API (Optional, for Chat feature) ===
# Get your API key from: https://aistudio.google.com/app/apikeys
GOOGLE_GENAI_API_KEY=your_api_key_here

# === QUICK START ===
# 1. Copy this file:  cp .env.example .env
# 2. Fill DATABASE_URL with your PostgreSQL connection string
# 3. Run migrations:  npx prisma migrate deploy
# 4. Seed test data:  npm run seed
# 5. Start server:    npm start

